# 六點輸入法（6d-IME）

NVDA 附加元件，提供六點點字鍵盤多模式輸入，支援注音點字、電腦點字（comp8）、UEB 英文點字，以及直接輸出 Unicode 點字字元。

## 需求

- NVDA 2026.1 以上（64 位元）
- Windows 10 / 11
- 已安裝微軟注音輸入法（注音點字模式需要）

## 鍵位配置

六點鍵使用標準點字鍵盤佈局：

| 按鍵 | 點位 |
|------|------|
| F    | 點 1 |
| D    | 點 2 |
| S    | 點 3 |
| J    | 點 4 |
| K    | 點 5 |
| L    | 點 6 |
| 空白 | 確認／送出 |
| A    | 點 7（退格） |
| ;    | 點 8（Enter） |

點字顯示器也可直接輸入。

## 快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| Ctrl+NVDA+7 | 開啟／關閉六點輸入 |
| J+K+L+Space | 在已勾選的點字模式間循環切換 |
| Ctrl+NVDA+8 | 開啟模式偏好設定 |

關閉時切換回一般輸入法；開啟時進入偏好設定中第一個勾選的模式。

切換模式時 J+K+L 按住不放，持續按 Space 可連續切換。

## 輸入模式

### 注音點字

使用臺灣注音點字輸入中文。點字格對應規則來自 `Phn.tbl` 對照表。

- 輸入聲母＋韻母自動送出，再以調號確認
- 標點符號以點字格序列輸入：
  - 雙格序列（如 ⠒⠒ = ：）輸入第二格時立即送出
  - 支援標點符號輸入，單格標點輸入後需按空白鍵

### 黑點點字

直接輸出 Unicode 點字字元（U+2800–U+28FF），用於需要插入點字符號的場合。

### 電腦點字（comp8）

使用 `en-us-comp8-ext.utb` 對照表輸入英文及符號。每格即時送出對應字元，Space 送出空格。

### UEB 一級

使用 `en-ueb-g1.ctb` 對照表，字母逐格翻譯後即時送出。

### UEB 二級

使用 `en-ueb-g2.ctb` 縮寫對照表，整個單字輸入完畢後按 Space 一次送出翻譯結果。

## 偏好設定

Ctrl+NVDA+8 開啟模式選擇對話框，勾選 J+K+L+Space 循環中要包含的點字模式。至少需勾選一個模式。設定儲存於 NVDA 設定檔（`[6dIME]` 區段）。

## 已知限制

從注音模式切換到其他模式時，若 IME 組字區有尚未確認的字（已送出注音鍵但未按調號或 Space），插件會自動送出 Enter 強制確認。若確認失敗（視窗不支援 IMM32，僅支援 TSF），組字區內容可能殘留在下一段輸入的前面，需手動刪除。